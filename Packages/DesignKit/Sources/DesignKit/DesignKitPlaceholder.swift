import Foundation

/// Placeholder. Design tokens and components are out of scope for this task.
///
/// When this package is filled in, `ScreenChrome` in RouterKit is the only place
/// navigation-bar styling may be read from, and no component may import RouterKit.
public enum DesignKitPlaceholder {
    public static let isStub = true
}
