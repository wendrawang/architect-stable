
/// Placeholder. Transport is out of scope for this task.
///
/// When this package is filled in, every operation is `async` and every response is
/// `Codable`; DTOs never leave the data layer of the feature that owns them.
public enum NetworkKitPlaceholder {
    public static let isStub = true
}
